#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    int a;
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> a;
    cout << a << endl;
    float f = 1.23563;
    cout << setiosflags(ios::fixed) << setprecision(5) << endl;
    cout << setiosflags(ios::fixed) << setprecision(4) << endl;
    int n;
    cin >> n;
    vector<int> d(n + 1, 0);
    vector<vector<int>> matrix(n + 1, vector<int>(n + 1, 0));
    multiset<int> s = {1, 2, 3, 4};

    return 0;
}